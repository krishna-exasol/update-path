# Shell scripts must stay LF even on Windows checkouts (core.autocrlf=true
# otherwise rewrites them to CRLF, which breaks bash when the clone is run
# from WSL via /mnt/c or copied into a Linux environment).
*.sh text eol=lf

# The exakit helper is an extensionless bash script, so the *.sh rule cannot
# match it. A CRLF checkout installs a helper whose shebang is broken
# (env: 'bash\r': No such file or directory) for WSL installs that use a
# Windows-side checkout (EXAKIT_LOCAL_KIT or /mnt/c).
setup/exakit text eol=lf

# The versions manifest is read line by line (sed/awk) when no Python runtime
# is available, and its canonical form is CI-checked. A CRLF checkout would
# leave a CR on every extracted value — enough to break a download URL.
versions.json text eol=lf

# Runtime-parsed text files. The bash installer reads dataset.conf with sed
# (a CRLF checkout makes every dataset id end in CR, so dataset discovery
# fails with "Unknown bundled dataset"), and the SQL/CSV fixtures are fed to
# the database, which expects LF row terminators.
*.conf text eol=lf
*.sql text eol=lf
*.py text eol=lf
*.csv text eol=lf

# install.sh fetches the repo as a source tarball, so every byte in the tree is
# on the critical path of `curl | sh` — before the banner even appears. The demo
# video is documentation: nothing in the kit reads it, and no installed copy
# needs it. export-ignore keeps it in the repo (browsable, cloneable, linkable)
# while dropping it from the archive GitHub builds with `git archive`, which
# takes the installer download from ~27 MB to ~7 MB.
static/*.mp4 export-ignore
